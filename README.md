# ROI RGB/HSV/Gray + PLS + DD-SIMCA

App web para:

- seleção de ROI;
- cálculo RGB;
- HSV;
- grayscale;
- PLS;
- DD-SIMCA.

## Rodar localmente

```bash
pip install -r requirements.txt
streamlit run app.py
